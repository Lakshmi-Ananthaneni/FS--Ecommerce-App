# Fullstack E-commerce Project
## Installation

1. `api:`  backend lives
2. `client:` Frontend of  application

To install,  need to go to both directories and install their packages as such:

```bash
cd api
yarn install
```

```bash
cd client
yarn install
```

To run the `frontend` : yarn start
To run the `backend` : yarn start:dev








